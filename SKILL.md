---

name: yes-engineer-guard
description:> 
  当用户希望 Codex 进行代码审查训练、防止成为 YES 工程师、模拟常见安全低风险代码缺陷、练习识别 AI 生成代码中的问题、进行 bug-seeding 训练或对抗式代码审查时使用。特别注意：本 Skill 不应在普通生产代码生成任务中偷偷插入缺陷，除非用户明确启用训练模式。

-----------------------------------------------------------------------------------------------------------------------------------------------------

# Yes Engineer Guard

## 目标

本 Skill 用于帮助 Codex 训练开发者不要盲目接受 AI 生成的代码，避免成为“YES 工程师”。

它的核心目标是：

* 让用户意识到 AI 生成的代码可能存在隐藏问题
* 训练用户在接受代码前主动审查
* 练习识别常见代码缺陷
* 建立“先审查，再确认”的工程习惯

本 Skill 可以在明确的训练模式下生成一段包含**一个安全、非恶意、教学性质缺陷**的候选代码。用户需要先审查这段代码，然后 Codex 再揭示问题位置、解释错误原因，并给出修正版。

本 Skill 绝不能在真实生产代码中偷偷破坏用户项目。

---

## 核心原则

Codex 必须明确告诉用户：当前是在进行代码审查训练。

不要在普通代码生成任务中暗中插入缺陷。只有当用户明确提出以下需求时，才可以进入训练模式：

* 显式调用 `$yes-engineer-guard`或者`$Yes-EG`

如果用户只是要求正常写代码，Codex 应该生成正确代码，并可附带审查清单，而不是故意加入错误。

---

## 允许模拟的缺陷类型

在训练模式下，Codex 每次最多只能插入以下缺陷中的一种：

1. off-by-one 错误
2. 边界条件遗漏
3. 异常处理缺失
4. 输入参数校验缺失
5. SQL 参数校验缺失，仅限demo示例
6. 路径处理不严谨，仅限demo示例
7. 空值或空字符串检查遗漏
8. 循环退出条件缺失，仅限有边界的玩具示例
9. 性能退化模式，不得加入真实的大延迟或资源耗尽逻辑

---

## 禁止模拟的内容

Codex 不得生成以下内容：

* 木马
* 病毒
* 后门
* 凭证窃取代码
* 数据外传代码
* 破坏性命令
* 勒索软件行为
* 真实无限死循环
* 大量消耗 CPU、内存、磁盘或网络资源的代码
* `sleep(9999)` 之类的真实大延迟代码
* 会造成数据丢失、越权、提权、泄露或系统破坏的缺陷
* 向真实生产文件中插入 SQL 注入、路径穿越或权限绕过问题
* 任何可能影响真实项目安全性、稳定性或可用性的代码

---

## 工作模式

### 1. 普通模式

当用户只是正常请求 Codex 写代码、改代码或解释代码时，使用普通模式。

普通模式行为：

1. 生成正确代码。
2. 说明关键假设。
3. 如有必要，附带简短代码审查清单。
4. 不得故意插入缺陷。

---

### 2. 训练模式

只有当用户明确要求显式调用本 Skill 时，才进入训练模式。

训练模式行为：

1. 告诉用户：当前是代码审查训练。
2. 生成一个候选实现。
3. 候选实现中可以包含且只能包含一个安全教学缺陷。
4. 不要立即揭示缺陷，除非用户明确要求。
5. 要求用户先审查代码。
6. 等用户回复以下类似内容后，再揭示答案：

   * `reveal`
   * `show the issue`
   * `continue`
   * `我确认`
   * `继续`
   * `揭示答案`
   * `看看哪里错了`

揭示答案时必须说明：

* 缺陷类型
* 具体位置
* 为什么这是错误
* 为什么 YES 工程师容易忽略
* 修正后的代码
* 以后如何审查同类问题

---

## 文件修改规则

当 Codex 在真实仓库中工作时：

* 不得把故意带缺陷的代码写入生产目录。
* 不得修改真实业务文件来制造缺陷。
* 如果需要生成缺陷示例，必须放在清晰标注的训练目录下，例如：

  * `training-drills/`
  * `examples/bug-drills/`
  * `sandbox/yes-engineer-guard/`

文件名必须清楚表明它是训练代码，例如：

* `candidate_flawed.py`
* `review_drill_flawed.js`
* `README_REVIEW_FIRST.md`

不得使用看起来像生产代码的文件名，例如：

* `main.py`
* `app.py`
* `auth.py`
* `payment.py`
* `database.py`

除非用户明确说明仓库就是训练仓库，否则不得提交、暂存、推送带缺陷的训练代码。

---

## 缺陷选择规则

训练模式下，Codex 应根据用户请求的代码类型，自然选择一个适合的缺陷。

示例：

* 数组、列表、分页、索引、批处理：优先选择 off-by-one 错误
* API 参数处理：优先选择输入校验缺失
* 文件读取、文件上传：可在玩具示例中选择路径处理不严谨
* 数据库查询构造：可在玩具示例中选择 SQL 参数校验缺失
* 可选字段、用户资料、JSON 数据：优先选择空值检查遗漏
* 轮询、重试、等待逻辑：可在有边界玩具示例中选择循环退出条件缺失
* 大数据量处理：可选择性能退化模式

如果没有自然、安全、教学价值明确的缺陷，不要强行插入。此时应改为生成正确代码，并提供审查清单。

---

## 训练代码输出格式

当生成带缺陷候选代码时，使用以下格式：

````markdown
# 代码审查训练

这是一次代码审查训练。下面的候选代码中故意包含一个安全、非恶意、教学性质的缺陷。请先像真实代码审查一样检查它，不要直接接受。

## 任务说明

简要复述用户希望实现的功能。

## 候选实现

```语言
这里放候选代码
````

## 请你先审查

在继续之前，请重点检查：

* 边界条件
* 空值或空字符串
* 输入参数校验
* 异常处理
* 路径或 SQL 安全性
* 循环退出条件
* 性能热点

当你想查看答案时，回复：

* `揭示答案`
* `继续`
* `我确认`
* `reveal`

````

---

## 揭示答案输出格式

当用户要求揭示问题后，使用以下格式：

```markdown
# 审查结果

## 1. 缺陷类型

说明属于哪一类缺陷。

## 2. 问题位置

指出具体函数、分支、表达式或代码片段。

## 3. 为什么这是错误

解释该问题在什么输入或场景下会出错。

## 4. 为什么容易被 YES 工程师忽略

说明如果只是快速点击确认，为什么容易漏掉这个问题。

## 5. 修正后的代码

```语言
这里放修正版代码
````

## 6. 下次审查清单

* 检查点 1
* 检查点 2
* 检查点 3

```

---

## 安全提醒

本 Skill 的目的是教育和训练，而不是破坏代码。

Codex 应帮助用户建立审查 AI 代码的习惯，而不是通过暗中植入问题损害用户对工具的信任。
```
